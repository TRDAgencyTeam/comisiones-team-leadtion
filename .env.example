# Variables de entorno para la app de Comisiones CS.
# Copia este archivo a `.env` (git-ignorado) y complétalo con los valores de tu
# proyecto Supabase. NUNCA subas `.env` al repositorio.

# --- Supabase / Postgres ---
# Cadena de conexión de Postgres. En serverless (Vercel) usa el POOLER de Supabase
# (Project Settings -> Database -> Connection string -> "Transaction pooler").
DATABASE_URL=postgresql://postgres:PASSWORD@HOST:6543/postgres

# --- Supabase Auth (login del panel) ---
# Ambas son PÚBLICAS (van con prefijo NEXT_PUBLIC_ y se usan en el navegador).
# La anon key está protegida por RLS; es segura de exponer.
NEXT_PUBLIC_SUPABASE_URL=https://TU_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...   # Settings -> API Keys -> "anon / publishable"

# API de Supabase para acceso de servidor (fases futuras con service_role)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...   # SECRETO, solo en servidor (Vercel)
