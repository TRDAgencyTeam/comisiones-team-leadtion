/** Las páginas de Nómina / Herramientas / Operativos viven bajo esta ruta, pero
 *  ya no muestran sub-navegación propia: se navegan desde el menú lateral madre. */
export default function GastosFijosLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
