{
  "name": "comisiones-cs-leadtion",
  "version": "0.3.0",
  "private": true,
  "description": "Plataforma de Comisiones CS (LEADTION) — monorepo (motor + app web)",
  "workspaces": [
    "engine",
    "web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "npm run test --workspace=comisiones-cs-engine",
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "esbuild@0.21.5": true,
    "sharp@0.34.5": true
  }
}
